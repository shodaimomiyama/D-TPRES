{
  "name": "formix-ao-hyperbeam",
  "version": "0.1.0",
  "description": "FORMIX AO contract deployment for HyperBEAM",
  "type": "module",
  "scripts": {
    "build": "cd contracts && cargo build --target wasm32-unknown-unknown --release",
    "deploy": "node scripts/deploy.js",
    "test": "node scripts/test.js"
  },
  "dependencies": {
    "@permaweb/aoconnect": "^0.0.93",
    "arweave": "^1.14.4"
  },
  "engines": {
    "node": ">=18"
  }
}
