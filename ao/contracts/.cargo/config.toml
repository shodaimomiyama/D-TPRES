[build]
# Default target for AO WASM builds
# Use wasm32-unknown-unknown until wasm64 compatibility is confirmed
target = "wasm32-unknown-unknown"

[target.wasm32-unknown-unknown]
rustflags = ["-C", "link-arg=-zstack-size=65536"]
